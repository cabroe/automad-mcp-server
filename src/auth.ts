// Stub for Task 7. Re-exports AuthProvider so client.ts can be developed independently.
// Task 7 will replace this with the full AuthManager implementation.
export type { AuthProvider } from "./client.js";
export type Cookie = string;
